version: "3"

services:

  db:
    container_name: db
    image: postgres
    restart: always
    env_file:
      -  ../.env
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
